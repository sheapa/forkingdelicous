import React from 'react'
import { Input } from 'semantic-ui-react'

const InputField = () => (
  <Input action='Save' placeholder='Save' />
)
// const InputField = ({ q, handleInputChange, handleFormSubmit }) => (
//   <Input action='Save' placeholder='Save' />
// )

export default InputField;